{
  "name": "foundfootage-message-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "db:local": "wrangler d1 migrations apply foundfootage-messages --local",
    "db:remote": "wrangler d1 migrations apply foundfootage-messages --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.113.0"
  }
}
